spring.application.name=acervo-api
server.port=${PORT:8081}

# Define o perfil padr\u00E3o como 'prod' (Cloud Run)
spring.profiles.active=prod

# JWT - o segredo vem da vari\u00E1vel de ambiente configurada no GCP
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# Google Cloud Storage - bucket do Cloud Run
gcp.storage.bucket-name=run-sources-acervo-carmen-lydia-southamerica-east1
